// Crie 3 variáveis de tipos diferentes, utilize template strings para combinar os diferentes tipos de dados (number, string, boolean) em uma única string e
// exiba o resultado no console.

const number = 53;
const string = 'chover';
const boolean = true;

const resultado = `É verdade que fazem ${number} dias que não para de ${string}? ${boolean}`;
console.log(resultado);
