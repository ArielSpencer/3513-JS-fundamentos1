// Crie uma variável contendo uma frase. Em seguida, exiba no console o comprimento da frase e a mesma frase em constras maiúsculas.

const frase = 'Tudo o que temos de decidir é o que fazer com o tempo que nos é dado';

console.log(`A frase possui ${frase.length} caracteres`);
console.log(frase.toUpperCase());
