// Declare duas variáveis, uma contendo um número e outra contendo uma string.
// Em seguida, combine-as em uma terceira variável usando template strings para montar uma frase e
// exiba o resultado no console.

const numero = 365;
const string = 'World';
const worldName = `O projeto ${string} deve ser concluído em ${numero} dias.`;

console.log(worldName);